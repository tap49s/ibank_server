server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:mysql://db-mysql-nyc3-32977-do-user-16182263-0.m.db.ondigitalocean.com:25060/defaultdb
    username: doadmin
    password: AVNS_RAvkBVBBe1taqTd_b7s
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # Creates or updates the database schema automatically
    show-sql: true      # Logs SQL statements
    properties:
      hibernate:
        format_sql: true # Formats the SQL logs
        dialect: org.hibernate.dialect.MySQL8Dialect
logging:
  level:
    org.springframework.security: DEBUG
